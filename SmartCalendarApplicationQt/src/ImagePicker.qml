import QtQuick 2.0
import QtQuick.Controls 2.3

// choosesa suitable image picker depending on the platform
// and

Menu {
        id: menu
        MenuItem { text: "Gallery" }
        MenuItem { text: "Camera" }

    }

